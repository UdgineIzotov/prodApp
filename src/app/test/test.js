/* import { PubSub } from '../Pubsub';

require('../fakerMaker');
require('../JQueryPlugins/tabs');
require('../JQueryPlugins/tooltip');

import { DataStorage } from '../model/taskDB';

export default function run () {
    const main = document.querySelector('main');
    main.innerHTML = (require('./test.hbs'))();

    setTimerDucation(60);

    $('.radial-timer').addClass('play');

    $('.radial-timer').click(() => {
        console.log('lalala');
        $('.radial-timer').removeClass('play');
    });
}


function setTimerDucation (time) {
    document.querySelector('.radial-timer-half:nth-of-type(1) .after').style.animationDuration = `${time}s`;
    document.querySelector('.radial-timer-half:nth-of-type(2) .after').style.animationDuration = `${time}s`;
}
*/
